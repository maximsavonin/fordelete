#define _USE_MATH_DEFINES




using namespace std;

struct Point
{
   int x, y;
};

void change_figure(Point romb[2], int i, double &fi)
{
   if(i < 5)
   {
      int r;
      if(i % 2 == 0)
      {
         r = romb[1].x - sqrt(pow(mousex()-_abracadabra_cast(romb[1]);